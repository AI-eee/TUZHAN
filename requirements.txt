# TUZHAN 项目依赖
# 由于项目基于 Markdown 文件处理与交互，预先引入一些可能用到的基础库
markdown2==2.4.13
PyYAML==6.0.1
pydantic==2.6.4  # 用于数据结构校验和定义

# [修改原因]: 引入 FastAPI 提供 Web API 服务，让没有权限的人也能通过接口收发信息；引入 requests 给客户端程序使用。
fastapi==0.110.0
uvicorn==0.29.0
requests==2.31.0

# [修改原因]: 引入 jinja2 提供Web页面渲染；python-multipart用于处理表单数据
jinja2==3.1.3
python-multipart==0.0.9
bleach==6.2.0  # [新增原因]: 用于在渲染 Markdown 转换为 HTML 时清洗不安全的标签防 XSS

# [修改原因]: 引入 openai 兼容库以调用阿里百炼大模型；升级到 >=1.50.0 版本以解决旧版本对高版本 httpx (>=0.28.0) 移除 proxies 参数造成的初始化报错。引入 python-dotenv 读取 .env
openai>=1.50.0
python-dotenv==1.0.1
slowapi==0.1.9
